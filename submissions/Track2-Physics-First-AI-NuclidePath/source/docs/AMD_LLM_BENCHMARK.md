# AMD ROCm LLM benchmark

**Scope note:** this is the dated 28 July 2026 core-platform/LLM snapshot. It supports the recorded core video; the **5 August 2026 post-merge rerun of `main` at head `b6d4c61e74`** (388 passed, 2 skipped) is archived under `artifacts/amd-2026-08-05/post-merge/`.

## Reproducibility record

- Timestamp UTC: `2026-07-19T19:53:44Z`
- GPU: AMD Radeon Graphics, `gfx1100`
- VRAM reported by llama.cpp: `49136 MiB`
- ROCm: `7.2.1`
- llama.cpp source revision: `571d0d5`
- Build: HIP/ROCm, `GPU_TARGETS=gfx1100`, Release
- Model: `unsloth/Qwen3.5-9B-GGUF`
- File: `Qwen3.5-9B-Q8_0.gguf`
- File size: `9,527,502,048` bytes (`8.86 GiB` as reported by llama.cpp)
- SHA-256: `809626574d0cb43d4becfa56169980da2bb448f2299270f7be443cb89d0a6ae4`
- GPU offload: `-ngl 99`
- Server context: `8192`
- Batch size: llama.cpp default for this benchmark
- Concurrent slots: server default `4`; benchmark used one request at a time

## Native benchmark

Command:

```bash
./build-rocm/bin/llama-bench \
  -m /workspace/models/Qwen3.5-9B-Q8_0.gguf \
  -ngl 99 -p 512 -n 128 -r 2
```

Measured on the AMD GPU:

| Test | Throughput |
|---|---:|
| Prompt processing, 512 tokens | `2861.56 ± 178.91 tokens/s` |
| Token generation, 128 tokens | `67.77 ± 0.09 tokens/s` |

## OpenAI-compatible server

Server command:

```bash
./build-rocm/bin/llama-server \
  -m /workspace/models/Qwen3.5-9B-Q8_0.gguf \
  -ngl 99 -c 8192 \
  --host 127.0.0.1 --port 8000 \
  --alias qwen35-9b-q8 \
  --jinja --reasoning off --metrics
```

Observed API request:

- prompt tokens: `34`
- generated tokens: `13`
- end-to-end latency: `0.383 s`
- prompt throughput: `438.80 tokens/s`
- generation throughput: `69.32 tokens/s`

The lower prompt throughput compared with `llama-bench pp512` is expected because the API request had only 34 prompt tokens and includes server/request overhead.

## Full workflow integration result

The real local model generated the complete allow-listed plan:

```json
[
  "validate_permissions",
  "site_agent",
  "knowledge_retrieval",
  "environment_agent",
  "store_memory",
  "synthesize_report"
]
```

A historical full run with 128 seeded uncertainty samples completed in `1.007 s` using `transport-prototype-0.2`; retain it only as historical context. The current `0.3` rerun is documented below.

```text
K+ 0 mg/L:
  Kd_eff = 0.2 m³/kg
  R = 972.4286
  5e9 s  -> 23403.20 Bq/m³
  1e10 s ->   688.88 Bq/m³

K+ 20 mg/L:
  Kd_eff = 0.1666667 m³/kg
  R = 810.5238
  5e9 s  -> 24733.24 Bq/m³
  1e10 s ->   681.43 Bq/m³
```

The LLM-planned and deterministic-offline reports had identical physical `runs` arrays for the same scenario. The LLM is restricted to planning; all numerical values are generated by deterministic code. This is an integration and software-verification result, not radiological safety validation.

## Security note

The server is bound to `127.0.0.1` and has no API key because it is used only inside the remote workspace. It must not be exposed publicly without authentication and restricted CORS.

## Final `transport-prototype-0.3` AMD rerun

The current release was rerun on 28 July 2026 UTC using the same `gfx1100`, ROCm 7.2.1, llama.cpp revision `571d0d5`, Qwen3.5-9B Q8_0 model hash, `-ngl 99`, and an 8192-token loopback server context. The source and model hashes matched the controller copies before execution.

Native benchmark command:

```bash
./build-rocm/bin/llama-bench \
  -m /workspace/models/Qwen3.5-9B-Q8_0.gguf \
  -ngl 99 -p 512 -n 128 -r 3
```

| Final `0.3` measurement | Throughput |
|---|---:|
| Prompt processing, 512 tokens | `2877.30 ± 153.36 tokens/s` |
| Token generation, 128 tokens | `67.66 ± 0.14 tokens/s` |

The real OpenAI-compatible planner request processed 573 prompt tokens at `1906.31 tokens/s` and generated 1304 tokens at `66.68 tokens/s`; server-reported total request time was `19.85590 s`. The complete LLM-planned 128-sample NuclidePath pipeline took `19.9692 s`; the matching deterministic-offline run took `0.0635364 s`.

The local model returned the complete six-step allow-listed workflow. For the same input and seed, the LLM-planned and offline outputs were identical for scenario, potassium comparison, deterministic physical runs, assumptions, warnings, sensitivity, and uncertainty. All five Track 2 capability flags were true. At this retained historical LLM checkpoint, the AMD workspace passed `152` tests and the controller passed `147` with `3` expected optional-PyTorch skips. These counts are not the current release gate; the latest recorded core checkpoints pass `231` on AMD/PyTorch and `216` with `13` optional-PyTorch skips on the dependency-light controller.

Reproducibility evidence is committed under `artifacts/amd-2026-07-28/llm/`, including hashes, native benchmark output, server log, planner/offline artifacts, manifests, execution times, and the parity record. These are software and performance verification results, not site-specific or regulatory validation.
